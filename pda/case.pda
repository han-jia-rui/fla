; This pda program implements parenthesis matching problem

; Example Input: ((())) 
; Result: true

; States set
#Q = {q0,accept}

; Input alphabet
#S = {(,)}

; Stack alphabet
#G = {1,z}

; Start state
#q0 = q0

; Start stack symbol
#z0 = z

; Final states
#F = {accept}

; the transition functions
q0 ( z q0 1z
q0 ( 1 q0 11
q0 ) 1 q0 _
q0 _ z accept _
